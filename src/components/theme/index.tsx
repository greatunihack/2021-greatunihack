import { createTheme } from "@material-ui/core";

const Theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#00ffb0",
    },
    secondary: {
      main: "#ddd92a",
    },
  },
});

export default Theme;
