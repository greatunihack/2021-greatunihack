import { createTheme } from "@material-ui/core";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#30362f",
    },
    secondary: {
      main: "#ddd92a",
    },
  },
});

export default Theme;
